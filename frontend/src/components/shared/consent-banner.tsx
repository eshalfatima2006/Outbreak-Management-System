"use client";

/** Stub for consent linking. Replace with real implementation. */
export async function linkConsentToEmail(_email: string): Promise<void> {
  // MOCK DATA - Replace with real API data
  return Promise.resolve();
}